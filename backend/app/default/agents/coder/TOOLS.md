# Tools

## MCP
- linear: https://mcp.linear.app/mcp (read tickets / post comments / change status)
- (Phase 4) jira

## Local commands
- git, gh, pnpm, npm, yarn
- vitest, jest, pytest
- eslint, ruff

## Memory tools (injected by backend)
- memory_search(query)
- memory_get(path, range?)

## Forbidden
- rm -rf outside the workspace
- git push --force on any branch
- Commands that operate on the host system outside the workspace
