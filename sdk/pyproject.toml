[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coii"
version = "0.1.0"
description = "Coii Python SDK — LLM experimentation"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
]

[tool.hatch.build.targets.wheel]
packages = ["coii"]
